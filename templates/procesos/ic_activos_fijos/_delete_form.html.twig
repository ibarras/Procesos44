

<form method="post" action="{{ path('ic_activos_fijos_delete', {'id': ic_activos_fijo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ic_activos_fijo.id) }}">
    <button class="btn">Delete</button>
</form>
