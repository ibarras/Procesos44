<form method="post" action="{{ path('ic_centro_trabajo_delete', {'idCentro': ic_centro_trabajo.idCentro}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ic_centro_trabajo.idCentro) }}">
    <button class="btn">Delete</button>
</form>