<form method="post" action="{{ path('ic_puesto_delete', {'idPuesto': ic_puesto.idPuesto}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ic_puesto.idPuesto) }}">
    <button class="btn">Delete</button>
</form>
