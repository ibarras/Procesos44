<form method="post" action="{{ path('ic_documentos_perfil_delete', {'id': ic_documentos_perfil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ic_documentos_perfil.id) }}">
    <button class="btn">Delete</button>
</form>
