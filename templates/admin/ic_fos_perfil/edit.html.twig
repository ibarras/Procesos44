{% extends 'base.html.twig' %}

{% block title %}Direccion {% endblock %}

{% block content %}

    <div class="row">

        <div class="col-md-6 col-md-offset-1">

            <div class="card">
                <div class="card-header">
                    <h1>Perfil de usuario</h1>
                </div>
                <div class="card-body">
                    {{ include('admin/ic_fos_perfil/_form.html.twig', {'button_label': 'Actualizar'}) }}
                    {{ include('admin/ic_fos_perfil/_delete_form.html.twig') }}

                </div>
                <div class="card-footer">
                    <a href="{{ path('procesos_perfil') }}">Regresar</a>
                </div>
            </div>
        </div>
    </div>



{% endblock %}
