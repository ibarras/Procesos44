{% extends 'base.html.twig' %}

{% block content %}

<div class="row">

    <div class="col-md-10 col-md-offset-1">
        <div class="card-header"><h3>Editar Procedimiento</h3></div>
        <div class="card-body">
            {{ include('admin/ic_procedimientos/_form.html.twig', {'button_label': 'Actualizar'}) }}
            {{ include('admin/ic_procedimientos/_delete_form.html.twig') }}

        </div>
        <div class="card-footer">
            <a href="{{ path('procesos_procedimientos', {'id': ic_procedimiento.idProcesos.idMacroprocesos.id } ) }}">Regresar</a>
        </div>
    </div>
</div>

{% endblock %}
