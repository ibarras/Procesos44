{% extends 'base.html.twig' %}

{% block title %}IcPuesto{% endblock %}

{% block body %}
    <h1>IcPuesto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPuesto</th>
                <td>{{ ic_puesto.idPuesto }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ic_puesto.nombre }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ ic_puesto.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ic_puesto_index') }}">back to list</a>

    <a href="{{ path('ic_puesto_edit', {'idPuesto': ic_puesto.idPuesto}) }}">edit</a>

    {{ include('ic_puesto/_delete_form.html.twig') }}
{% endblock %}
